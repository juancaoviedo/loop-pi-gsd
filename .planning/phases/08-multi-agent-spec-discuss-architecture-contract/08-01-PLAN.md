---
phase: 08-multi-agent-spec-discuss-architecture-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow/orchestrator/debate-contract.mjs
  - flow/orchestrator/debate-policy.mjs
  - flow/orchestrator/debate-prototype.mjs
  - flow/cli.mjs
  - flow/tests/orchestrator-debate-prototype.test.mjs
autonomous: true
requirements:
  - INTD-06
  - CONS-01
user_setup: []
must_haves:
  truths:
    - D-01/D-11: Every debate event accepted by Phase 8 has the required envelope fields with per-event validation.
    - D-04/D-05/D-07: The same transcript and policy config always produce the same decision and sorted reason codes.
    - INTD-06: Agent A remains orchestration and decision authority; Agent B remains answer-only.
    - CONS-01: The deterministic prototype emits proceed only when confidence threshold is satisfied and no blockers exist.
  artifacts:
    - flow/orchestrator/debate-contract.mjs exists with event-family schema registry and major-version gate.
    - flow/orchestrator/debate-policy.mjs exists with pure deterministic decision function and stable reason ordering.
    - flow/orchestrator/debate-prototype.mjs exists with bounded-round transcript evaluator and structured output.
    - flow/tests/orchestrator-debate-prototype.test.mjs exists and asserts replay determinism.
    - flow/cli.mjs exposes a feature-flagged phase-8 prototype invocation seam.
  key_links:
    - flow/cli.mjs -> runDebatePrototype (feature-flag execution seam per D-09)
    - debate-contract validator -> debate-policy evaluator (validated input only)
    - debate-policy decision -> prototype result envelope (reason codes unchanged)
  prohibitions:
    - Do not persist chain-of-thought or hidden reasoning traces.
    - Agent B must not mutate final spec/discuss artifacts in Phase 8.
    - Orchestration must not continue when policy indicates block.
    - Logs must not include provider credentials or secret tokens.
  edge_coverage:
    covered:
      - R1 scope lock enforced in prototype input guardrails (spec/discuss only).
      - R2 role boundary enforced by event role checks.
      - R4 precedence deterministic when blockers and threshold compete.
      - R5 replay stability for decision and reason codes.
    backstop:
      - Detailed malformed/timeout/provider-failure matrix assertions are expanded in Plan 02.
    unresolved: []
---

## Phase Goal

**As a** Flow workflow operator, **I want to** run deterministic ask/answer contract evaluation for spec/discuss, **so that** multi-agent critique input can be used without losing policy control or replayability.

<objective>
Establish the first vertical slice of the Phase 8 architecture contract: failing replay test first, then minimal deterministic contract + policy + prototype wiring that passes for the happy path and role-boundary baseline.

Purpose: Deliver a runnable, deterministic contract seam that is limited to spec/discuss and enforces Agent A/Agent B boundaries from day one.
Output: Contract module, policy module, runnable prototype entry, and a deterministic replay test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-SPEC.md
@.planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-CONTEXT.md
@.planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-RESEARCH.md
@flow/orchestrator/interception.mjs
@flow/orchestrator/context-pack.mjs
@flow/lifecycle/answer-schema.mjs
@flow/governance/escalation-policy.mjs
</context>

<read_first>
- .planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-SPEC.md
- .planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-CONTEXT.md
- flow/lifecycle/answer-schema.mjs
- flow/governance/escalation-policy.mjs
- flow/tests/orchestrator-context-pack.test.mjs
</read_first>

## Artifacts this phase produces

- Function `validateDebateEventEnvelope` in `flow/orchestrator/debate-contract.mjs`
- Function `assertSupportedSchemaMajor` in `flow/orchestrator/debate-contract.mjs`
- Function `normalizeDebateTextFields` in `flow/orchestrator/debate-contract.mjs` (D-03)
- Function `evaluateDebateDecision` in `flow/orchestrator/debate-policy.mjs`
- Function `runDebatePrototype` in `flow/orchestrator/debate-prototype.mjs`
- CLI flag `--phase8-debate-prototype` in `flow/cli.mjs` (D-09)
- Decision output fields: `decision`, `reasonCodes`, `roundsConsumed`, `policySnapshot`, `schemaVersion`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing end-to-end prototype replay tests for happy-path determinism and role boundaries</name>
  <files>flow/tests/orchestrator-debate-prototype.test.mjs</files>
  <behavior>
    - Test 1: A valid spec/discuss transcript with Agent B answer event and confidence above threshold returns `decision=proceed`.
    - Test 2: Replaying the same transcript and config returns identical `decision` and identical sorted `reasonCodes` (D-07).
    - Test 3: A transcript containing Agent B control directive yields `escalate-and-block` and role-boundary reason code (INTD-06).
  </behavior>
  <action>Create `flow/tests/orchestrator-debate-prototype.test.mjs` using Node test runner fixtures that call `runDebatePrototype` and assert deterministic output shape, replay identity, and Agent A/Agent B authority constraints per D-04/D-05/D-07; keep assertions on structured fields only (no chain-of-thought content).</action>
  <verify>
    <automated>node --test flow/tests/orchestrator-debate-prototype.test.mjs</automated>
  </verify>
  <done>Tests fail initially for missing debate modules or unmet contract behavior and encode the expected deterministic output contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement minimal deterministic contract, policy, prototype, and feature-flagged CLI seam</name>
  <files>flow/orchestrator/debate-contract.mjs, flow/orchestrator/debate-policy.mjs, flow/orchestrator/debate-prototype.mjs, flow/cli.mjs</files>
  <behavior>
    - Valid envelope fields and supported schema major pass contract checks; unknown major fails closed (D-02/D-11).
    - Policy returns proceed only when blockers are empty and confidence meets threshold (D-04/D-05).
    - CLI prototype mode emits stable structured decision output fields (D-09/D-10).
  </behavior>
  <action>Implement `flow/orchestrator/debate-contract.mjs` with per-event validators over a shared envelope (D-01), semantic-major gate (D-02), and UTF-8 trim+NFC normalization function for free-text fields (D-03). Implement `flow/orchestrator/debate-policy.mjs` as a pure deterministic evaluator where blockers force `escalate-and-block` before threshold checks (D-04) and reason codes are sorted deterministically (D-07). Implement `flow/orchestrator/debate-prototype.mjs` to evaluate bounded transcript rounds and emit structured output per D-10. Update `flow/cli.mjs` to add a feature-flag execution path `--phase8-debate-prototype` limited to `/gsd-spec-phase` and `/gsd-discuss-phase` invocation contexts (D-09), rejecting out-of-scope workflows with explicit contract errors.</action>
  <verify>
    <automated>node --test flow/tests/orchestrator-debate-prototype.test.mjs</automated>
  </verify>
  <done>Prototype mode exists, deterministic replay passes, role boundaries are enforced, and happy-path proceed behavior is deterministic for valid transcripts.</done>
</task>

<task type="auto">
  <name>Task 3: Run targeted and suite-level regression checks for contract slice stability</name>
  <files>flow/tests/orchestrator-debate-prototype.test.mjs</files>
  <action>Execute focused prototype tests first, then run full `flow/tests/*.mjs` to confirm no regressions in interception, lifecycle retry, and governance escalation behavior while introducing debate-contract modules.</action>
  <verify>
    <automated>node --test flow/tests/orchestrator-debate-prototype.test.mjs && node --test flow/tests/*.mjs</automated>
  </verify>
  <done>All targeted and suite tests pass with deterministic outputs and no existing workflow regressions.</done>
</task>

</tasks>

<threat_model>
## Security Baseline

- `security_enforcement`: active
- `security_asvs_level`: 1
- `security_block_on`: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent transcript input -> contract validator | Untrusted event payload crosses into deterministic policy engine |
| CLI flag invocation -> prototype executor | Operator-controlled mode flags can route execution path |
| Prototype result -> governance/evidence consumers | Decision metadata can influence downstream escalation/finalization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | `debate-contract` envelope validation | high | mitigate | Fail closed on unknown major/event type and malformed required fields; block progression before policy evaluation. |
| T-08-02 | Elevation of Privilege | Agent role boundary in `debate-policy` | high | mitigate | Reject Agent B control directives and emit deterministic blocker reason; only Agent A path can set proceed/escalate state. |
| T-08-03 | Repudiation | Decision replay determinism | medium | mitigate | Sort reason codes and snapshot policy inputs to produce identical replay assertions for identical transcript/config. |
| T-08-04 | Information Disclosure | Prototype output/log fields | medium | mitigate | Restrict output to structured decision metadata and never emit hidden reasoning or provider secrets. |
| T-08-SC | Tampering | npm installs | low | accept | No package installation in this plan; package legitimacy gate remains satisfied from research with no new install action. |
</threat_model>

<verification>
- `node --test flow/tests/orchestrator-debate-prototype.test.mjs`
- `node --test flow/tests/*.mjs`
</verification>

<acceptance_criteria>
- Happy-path transcript produces `proceed` only when threshold and no blockers are both satisfied.
- Replay of identical transcript and config returns identical decision and reason code ordering.
- Role contract is explicit and enforceable: Agent A orchestrates/writes decision; Agent B is answer-only.
- CLI prototype seam is feature-flagged and scoped to spec/discuss contract usage only.
</acceptance_criteria>

<success_criteria>
- Phase 8 vertical slice is runnable end to end with deterministic output and no regression in existing flow tests.
</success_criteria>

<output>
Create `.planning/phases/08-multi-agent-spec-discuss-architecture-contract/08-01-SUMMARY.md` when done
</output>
