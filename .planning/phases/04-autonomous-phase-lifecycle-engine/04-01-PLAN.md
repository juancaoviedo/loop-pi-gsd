---
phase: 04-autonomous-phase-lifecycle-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow/lifecycle/engine.mjs
  - flow/lifecycle/retry-policy.mjs
  - flow/lifecycle/answer-schema.mjs
  - flow/lifecycle/verification.mjs
  - flow/lifecycle/status-ledger.mjs
  - flow/lifecycle/persist-lifecycle.mjs
  - flow/cli.mjs
  - flow/tests/lifecycle-engine.test.mjs
  - flow/tests/lifecycle-retry.test.mjs
  - flow/tests/lifecycle-verification-gate.test.mjs
autonomous: true
requirements:
  - PHASE-02
  - PHASE-03
  - PHASE-04
  - INTD-03
  - VER-01
  - VER-02
  - VER-03
must_haves:
  truths:
    - Selected phases progress through fixed lifecycle order (`discuss`, `plan`, `execute`, `verify`).
    - Failed lifecycle steps use bounded retry policy with deterministic terminal behavior.
    - Responder payload schema validation blocks invalid interactive resumes.
    - Verification output is machine-readable and required for phase completion.
    - Root `flow/` remains implementation location; gsd-pi remains thin bridge.
  artifacts:
    - flow/lifecycle/engine.mjs
    - flow/lifecycle/retry-policy.mjs
    - flow/lifecycle/answer-schema.mjs
    - flow/lifecycle/verification.mjs
    - flow/lifecycle/status-ledger.mjs
    - flow/lifecycle/persist-lifecycle.mjs
    - flow/cli.mjs
    - flow/tests/lifecycle-engine.test.mjs
    - flow/tests/lifecycle-retry.test.mjs
    - flow/tests/lifecycle-verification-gate.test.mjs
---

## Objective

Implement deterministic autonomous lifecycle execution in root `flow/` for selected phases, including bounded retries, schema validation for delegated answers, and verification completion gating.

## Execution Context

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-autonomous-phase-lifecycle-engine/04-SPEC.md
@.planning/phases/04-autonomous-phase-lifecycle-engine/04-CONTEXT.md
@flow/cli.mjs
@flow/orchestrator/select-phases.mjs
@flow/orchestrator/isolation.mjs
@flow/orchestrator/context-pack.mjs

## Tasks

1. Implement lifecycle sequencing and status ledger updates

<task type="auto" tdd="true">
  <name>Task 1: Execute deterministic lifecycle chain per selected phase</name>
  <files>flow/lifecycle/engine.mjs, flow/lifecycle/status-ledger.mjs, flow/tests/lifecycle-engine.test.mjs</files>
  <behavior>
    - Runs fixed chain order: discuss, plan, execute, verify.
    - Records machine-readable status entries after each step.
    - Emits per-phase lifecycle output structure for downstream persistence.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/lifecycle-engine.test.mjs</automated></verify>
</task>

2. Implement bounded retries and responder schema validation

<task type="auto" tdd="true">
  <name>Task 2: Enforce retry policy and delegated answer schema guards</name>
  <files>flow/lifecycle/retry-policy.mjs, flow/lifecycle/answer-schema.mjs, flow/tests/lifecycle-retry.test.mjs</files>
  <behavior>
    - Applies bounded retries for retryable lifecycle steps.
    - Fails closed after retry budget is exhausted.
    - Validates responder answer schema before blocked step resume.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/lifecycle-retry.test.mjs</automated></verify>
</task>

3. Implement verification outputs, evidence persistence, and completion gate

<task type="auto" tdd="true">
  <name>Task 3: Enforce verification gate and persist lifecycle artifacts</name>
  <files>flow/lifecycle/verification.mjs, flow/lifecycle/persist-lifecycle.mjs, flow/cli.mjs, flow/tests/lifecycle-verification-gate.test.mjs</files>
  <behavior>
    - Verify step emits stable machine-readable check results and aggregate verdict.
    - Evidence references are persisted per phase run.
    - Completion gate blocks `complete` status when verification is missing or failed.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/lifecycle-verification-gate.test.mjs</automated></verify>
</task>

## Verification

1. Run lifecycle sequencing tests.
2. Run retry/schema-validation tests.
3. Run verification-gate tests.
4. Run aggregate Flow tests for regression safety.

## Success Criteria

1. PHASE-02: fixed lifecycle chain runs deterministically for selected phases.
2. PHASE-03: lifecycle status and artifacts are updated after each step.
3. PHASE-04: bounded retry policy handles failed steps with deterministic terminal outcome.
4. INTD-03: invalid responder answers fail schema guard and block resume.
5. VER-01/02/03: machine-readable verification outputs and evidence are persisted; completion is blocked on failed/missing verification.

## Output

Create `.planning/phases/04-autonomous-phase-lifecycle-engine/04-01-SUMMARY.md` when done.

## PLANNING COMPLETE
