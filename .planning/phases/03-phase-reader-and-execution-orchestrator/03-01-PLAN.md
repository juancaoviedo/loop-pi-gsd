---
phase: 03-phase-reader-and-execution-orchestrator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow/orchestrator/select-phases.mjs
  - flow/orchestrator/isolation.mjs
  - flow/orchestrator/context-pack.mjs
  - flow/orchestrator/interception.mjs
  - flow/orchestrator/run-manifest.mjs
  - flow/cli.mjs
  - flow/tests/orchestrator-select.test.mjs
  - flow/tests/orchestrator-isolation.test.mjs
  - flow/tests/orchestrator-context-pack.test.mjs
autonomous: true
requirements:
  - PHASE-01
  - SAFE-02
  - INTD-01
  - INTD-02
must_haves:
  truths:
    - `/flow-execute-all-phases` deterministically reads roadmap phases and selects target phases.
    - Selected phases produce isolated execution descriptors under distinct run context paths.
    - Interactive interception metadata is generated for downstream lifecycle stages.
    - Responder context packs are deterministic and built only from allowlisted planning artifacts.
    - Root `flow/` remains the implementation location; gsd-pi stays thin bridge only.
  artifacts:
    - flow/orchestrator/select-phases.mjs
    - flow/orchestrator/isolation.mjs
    - flow/orchestrator/context-pack.mjs
    - flow/orchestrator/interception.mjs
    - flow/orchestrator/run-manifest.mjs
    - flow/cli.mjs
    - flow/tests/orchestrator-select.test.mjs
    - flow/tests/orchestrator-isolation.test.mjs
    - flow/tests/orchestrator-context-pack.test.mjs
---

## Objective

Implement deterministic orchestration entry behavior for `/flow-execute-all-phases` in root `flow/`: phase loading/selection, isolation descriptors, interception metadata, and deterministic responder context packs.

## Execution Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-phase-reader-and-execution-orchestrator/03-SPEC.md
@.planning/phases/03-phase-reader-and-execution-orchestrator/03-CONTEXT.md
@flow/cli.mjs
@flow/kernel/commands.mjs
@flow/kernel/policy.mjs

## Tasks

1. Implement deterministic roadmap phase reader and selector

<task type="auto" tdd="true">
  <name>Task 1: Load roadmap phases and select targets deterministically</name>
  <files>flow/orchestrator/select-phases.mjs, flow/tests/orchestrator-select.test.mjs</files>
  <behavior>
    - Reads roadmap phase definitions in declared order.
    - Applies deterministic selection policy (pending-only default, explicit filters supported).
    - Produces stable phase target list for identical roadmap/state input.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/orchestrator-select.test.mjs</automated></verify>
</task>

2. Implement per-phase isolated run descriptors

<task type="auto" tdd="true">
  <name>Task 2: Build isolated execution descriptors for selected phases</name>
  <files>flow/orchestrator/isolation.mjs, flow/orchestrator/run-manifest.mjs, flow/tests/orchestrator-isolation.test.mjs</files>
  <behavior>
    - Generates deterministic run ids and per-phase subcontext paths.
    - Ensures no path collisions across selected phases in a run.
    - Persists an orchestration run manifest for replayability.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/orchestrator-isolation.test.mjs</automated></verify>
</task>

3. Implement interception metadata and responder context packs

<task type="auto" tdd="true">
  <name>Task 3: Emit interactive interception metadata and deterministic context packs</name>
  <files>flow/orchestrator/interception.mjs, flow/orchestrator/context-pack.mjs, flow/cli.mjs, flow/tests/orchestrator-context-pack.test.mjs</files>
  <behavior>
    - Declares interception metadata for spec/discuss interactive rounds.
    - Builds deterministic responder context packs from allowlisted artifacts.
    - Exposes orchestration payload through `flow-execute-all-phases` CLI path.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/orchestrator-context-pack.test.mjs</automated></verify>
</task>

## Verification

1. Run selector determinism tests.
2. Run isolation descriptor uniqueness tests.
3. Run context-pack determinism and allowlist tests.
4. Run aggregate Flow tests for regression safety.

## Success Criteria

1. PHASE-01: deterministic roadmap phase selection implemented.
2. SAFE-02: selected phase execution descriptors are isolated and unique.
3. INTD-01: interactive interception metadata is emitted in orchestration payload.
4. INTD-02: responder context packs are deterministic and allowlist-bound.
5. Placement remains correct: implementation in root `flow/`, thin bridge only in gsd-pi.

## Output

Create `.planning/phases/03-phase-reader-and-execution-orchestrator/03-01-SUMMARY.md` when done.

## PLANNING COMPLETE
