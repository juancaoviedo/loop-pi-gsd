---
phase: 01-pi-integration-baseline-and-safe-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow/docs/phase-1-boundary.md
  - flow/README.md
  - flow/cli.mjs
  - flow/kernel/commands.mjs
  - flow/kernel/policy.mjs
  - flow/tests/kernel.test.mjs
  - external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.ts
  - external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.test.ts
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
  - CTRL-01
  - CTRL-02
  - CTRL-05
  - SAFE-01
must_haves:
  truths:
    - Flow owns intake normalization, canonical routing, and policy gating for the two accepted slash-command workflows, while Pi/gsd-pi owns execution authority and lifecycle machinery.
    - `flow-create-additional-phases` and `flow-execute-all-phases` are the only accepted canonical workflow ids and route through exact-match deterministic dispatch.
    - Unknown, malformed, or ambiguous slash input fails before workflow dispatch, and unauthorized tool/use-path combinations fail closed before execution begins.
    - Phase progression continues through existing Pi/gsd-pi workflow hooks and write/gate seams, with no duplicate lifecycle engine and no third workflow type introduced.
    - The phase remains thin and does not broaden the workflow catalog beyond the two canonical commands.
  artifacts:
    - flow/docs/phase-1-boundary.md
    - flow/README.md
    - flow/cli.mjs
    - flow/kernel/commands.mjs
    - flow/kernel/policy.mjs
    - flow/tests/kernel.test.mjs
    - external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.ts
    - external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.test.ts
  key_links:
    - slash-command text -> intake normalizer -> canonical workflow id
    - canonical workflow id -> root-level flow kernel -> policy check
    - root-level flow kernel -> existing gsd-pi workflow surfaces through thin adapter
    - thin adapter + existing seams -> phase progression without a Flow-owned lifecycle engine
---

## Objective

Lock Phase 1 to the thin deterministic kernel for Flow's two slash-command workflows: normalize intake, route only to the two canonical workflows, and keep execution inside existing Pi/gsd-pi authority and policy seams.

Purpose: Preserve the locked control-plane contract so later phases can add workflow behavior without changing the boundary, the routing rules, or the fail-closed policy surface.
Output: Root-level boundary contract doc, deterministic slash-command routing, fail-closed policy gates in `flow/`, and targeted tests that prove only the two accepted workflows are reachable.

## Execution Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pi-integration-baseline-and-safe-routing/01-SPEC.md
@.planning/phases/01-pi-integration-baseline-and-safe-routing/01-CONTEXT.md
@external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.ts
@flow/docs/phase-1-boundary.md
@flow/kernel/commands.mjs
@flow/kernel/policy.mjs
@flow/cli.mjs

## Context

Phase 1 is the control-plane kernel, not a new workflow engine. The plan must preserve the locked split from SPEC and CONTEXT: Flow owns intake normalization, canonical routing, and policy gating in a root-level `flow/` folder; Pi/gsd-pi owns execution authority, workflow surfaces, and lifecycle machinery.

The work should keep Flow-specific logic outside `external/gsd-pi` and leave only a thin adapter at the slash-command seam. No extra workflow catalog and no duplicate lifecycle implementation are allowed.

## Tasks

1. Boundary contract and command envelope

<task type="auto">
  <name>Task 1: Document the locked Flow/Pi boundary for Phase 1</name>
  <files>flow/docs/phase-1-boundary.md, flow/README.md</files>
  <action>Create the Phase 1 boundary contract in `flow/docs/` and document the root-level placement in `flow/README.md`. State that Flow logic lives in `flow/` and gsd-pi keeps only a thin adapter. Keep the contract narrow to the two canonical workflows and do not introduce a third workflow type or a separate lifecycle engine.</action>
  <verify><automated>test -s flow/docs/phase-1-boundary.md && grep -q 'flow-create-additional-phases' flow/docs/phase-1-boundary.md && grep -q 'flow-execute-all-phases' flow/docs/phase-1-boundary.md</automated></verify>
  <done>The boundary document exists and explicitly names the ownership split plus the two canonical Flow workflows.</done>
</task>

2. Deterministic intake and routing

<task type="auto" tdd="true">
  <name>Task 2: Normalize slash-command intake into exact-match canonical routes</name>
  <files>flow/cli.mjs, flow/kernel/commands.mjs, flow/tests/kernel.test.mjs, external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.ts, external/gsd-pi/packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.test.ts</files>
  <behavior>
    - `flow-create-additional-phases` and `flow-execute-all-phases` resolve to one canonical workflow id each.
    - Unknown, malformed, or ambiguous slash input does not dispatch a workflow.
    - Routing is exact and deterministic; identical input always selects the same workflow path.
  </behavior>
  <action>Implement canonical normalization in `flow/kernel/commands.mjs` and expose a minimal root CLI entrypoint in `flow/cli.mjs`. Keep gsd-pi as a thin adapter that forwards `/flow-*` slash commands to the root CLI. Add tests proving stable route selection and pre-dispatch rejection for malformed input.</action>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/kernel.test.mjs && cd external/gsd-pi && node --import ./src/resources/extensions/gsd/tests/resolve-ts.mjs --experimental-strip-types --test packages/gsd-agent-modes/src/modes/interactive/slash-command-handlers.test.ts</automated></verify>
  <done>The two canonical workflows route deterministically, and malformed or ambiguous input fails before dispatch.</done>
</task>

3. Policy gates and workflow surfaces

<task type="auto" tdd="true">
  <name>Task 3: Enforce fail-closed policy gates in the root Flow kernel while preserving Pi-owned workflow surfaces</name>
  <files>flow/kernel/policy.mjs, flow/tests/kernel.test.mjs</files>
  <behavior>
    - Disallowed tool and use-path combinations fail before execution starts.
    - Workflow progression continues through existing Pi/gsd-pi workflow hooks and write/gate mechanisms.
    - No duplicate lifecycle state machine is introduced in Flow-specific code.
  </behavior>
  <action>Implement fail-closed policy helpers in `flow/kernel/policy.mjs` with allowlist-based enforcement keyed by canonical workflow ids. Keep phase progression on existing Pi/gsd-pi workflow surfaces through the thin adapter and avoid any new lifecycle loop or workflow catalog expansion.</action>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/kernel.test.mjs</automated></verify>
  <done>Unsafe execution paths are blocked, and progression still runs through the existing Pi/gsd-pi workflow surfaces.</done>
</task>

## Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slash-command text -> intake normalizer | Untrusted user input is normalized into canonical workflow ids here. |
| Flow router -> root Flow kernel | Control-plane routing normalizes and validates canonical ids in `flow/`. |
| gsd-pi thin adapter -> Flow CLI | Slash-command adapter forwards to root Flow command entrypoint. |
| Flow policy helper -> execution handoff | Tool eligibility is checked before orchestration handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | Slash-command routing | high | mitigate | Use exact-match normalization and a canonical workflow registry so malformed or ambiguous commands cannot select a different workflow path. |
| T-1-02 | Elevation | Flow policy helper | high | mitigate | Fail closed on any tool/use-path combination not explicitly allowlisted per canonical workflow id. |
| T-1-03 | Repudiation | Phase progression hooks | medium | mitigate | Keep phase transitions inside existing Pi/gsd-pi workflow hooks and test that completion still depends on the upstream write/gate mechanism. |

## Verification

1. Run a structural diff check to catch malformed edits and whitespace issues early.
2. Run the root Flow kernel tests for canonical routing and fail-closed policy behavior.
3. Run the targeted gsd-pi slash-command adapter tests to prove bridge behavior stays deterministic.

## Success Criteria

1. BOOT-01, BOOT-02, and CTRL-01 are satisfied because the Phase 1 control-plane kernel normalizes input and deterministically routes the two canonical Flow workflows through the approved dispatch path.
2. CTRL-02 and SAFE-01 are satisfied because root Flow policy helpers fail closed on unauthorized tool or use-path combinations before execution begins.
3. CTRL-05 is satisfied because phase progression still uses Pi/gsd-pi workflow surfaces through a thin adapter instead of a duplicate lifecycle engine.
4. The phase remains thin: no extra workflow catalog, no broadened command surface, and no prompt-owned control flow.
5. The phase is falsifiable: routing determinism and policy rejection are both covered by explicit tests at the existing seams.

## Output

Create `.planning/phases/01-pi-integration-baseline-and-safe-routing/01-01-SUMMARY.md` when done.

## PLANNING COMPLETE
