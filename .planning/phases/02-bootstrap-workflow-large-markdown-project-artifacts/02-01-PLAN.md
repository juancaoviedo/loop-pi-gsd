---
phase: 02-bootstrap-workflow-large-markdown-project-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flow/bootstrap/ingest.mjs
  - flow/bootstrap/model.mjs
  - flow/bootstrap/phase-sizing.mjs
  - flow/bootstrap/merge-planning.mjs
  - flow/bootstrap/trace-map.mjs
  - flow/cli.mjs
  - flow/tests/bootstrap.test.mjs
  - flow/tests/bootstrap-phase-sizing.test.mjs
  - flow/tests/bootstrap-idempotence.test.mjs
  - flow/tests/bootstrap-traceability.test.mjs
autonomous: true
requirements:
  - BOOT-03
  - BOOT-04
must_haves:
  truths:
    - `/flow-create-additional-phases` loads a supported source document (Markdown, LaTeX, PDF, HTML) and deterministically produces or updates the four core `.planning/` artifacts.
    - Phase count and boundaries are computed by deterministic complexity sizing with bounded controls (`min_phases`, `max_phases`, `target_complexity_per_phase`).
    - Bootstrap merge behavior is idempotent for the same input.
    - Source section references are persisted into a trace map linked to generated artifact updates.
    - Root `flow/` remains the implementation location for bootstrap logic; gsd-pi remains a thin adapter.
  artifacts:
    - flow/bootstrap/ingest.mjs
    - flow/bootstrap/model.mjs
    - flow/bootstrap/phase-sizing.mjs
    - flow/bootstrap/merge-planning.mjs
    - flow/bootstrap/trace-map.mjs
    - flow/cli.mjs
    - flow/tests/bootstrap.test.mjs
    - flow/tests/bootstrap-phase-sizing.test.mjs
    - flow/tests/bootstrap-idempotence.test.mjs
    - flow/tests/bootstrap-traceability.test.mjs
---

## Objective

Implement the first production bootstrap workflow in root `flow/` so large source documents (Markdown, LaTeX, PDF, HTML) can produce coherent `.planning` artifacts with source traceability.

## Execution Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bootstrap-workflow-large-markdown-project-artifacts/02-SPEC.md
@.planning/phases/02-bootstrap-workflow-large-markdown-project-artifacts/02-CONTEXT.md
@flow/cli.mjs
@flow/kernel/commands.mjs
@flow/kernel/policy.mjs

## Tasks

1. Build deterministic bootstrap ingest + model pipeline

<task type="auto" tdd="true">
  <name>Task 1: Parse supported source formats into normalized bootstrap model</name>
  <files>flow/bootstrap/ingest.mjs, flow/bootstrap/model.mjs, flow/tests/bootstrap.test.mjs</files>
  <behavior>
    - Source document input supports Markdown (`.md`), LaTeX (`.tex`), PDF (`.pdf`), and HTML (`.html`, `.htm`).
    - Format-specific extraction is segmented into deterministic section units.
    - A normalized intermediate model captures project intent, constraints, and candidate phase directives.
    - Invalid or empty source fails with explicit errors.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/bootstrap.test.mjs</automated></verify>
</task>

2. Implement deterministic phase sizing and partitioning rules

<task type="auto" tdd="true">
  <name>Task 2: Compute phase count and boundaries from normalized work items</name>
  <files>flow/bootstrap/phase-sizing.mjs, flow/bootstrap/model.mjs, flow/tests/bootstrap-phase-sizing.test.mjs</files>
  <behavior>
    - Work items are scored by deterministic complexity dimensions (breadth, dependency depth, risk/uncertainty, verification surface).
    - Phase count is computed from `target_complexity_per_phase` and clamped to `min_phases`/`max_phases`.
    - Output phase boundaries are stable for identical source and configuration.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/bootstrap-phase-sizing.test.mjs</automated></verify>
</task>

3. Implement artifact merge and idempotence rules

<task type="auto" tdd="true">
  <name>Task 3: Deterministically create/update `.planning` artifacts</name>
  <files>flow/bootstrap/merge-planning.mjs, flow/cli.mjs, flow/tests/bootstrap-idempotence.test.mjs</files>
  <behavior>
    - `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` are created or updated from the normalized model.
    - Existing roadmap structure is preserved unless targeted additive updates are specified.
    - Re-running with identical source produces stable output (idempotent).
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/bootstrap-idempotence.test.mjs</automated></verify>
</task>

4. Add traceability map persistence

<task type="auto" tdd="true">
  <name>Task 4: Persist BOOT-04 source-to-artifact trace map</name>
  <files>flow/bootstrap/trace-map.mjs, flow/tests/bootstrap-traceability.test.mjs</files>
  <behavior>
    - Trace output maps source section ids/headings to artifact update targets.
    - Trace map is written per run and references all generated/updated artifact sections.
  </behavior>
  <verify><automated>cd /home/juan/codes/loop-pi-gsd && node --test flow/tests/bootstrap-traceability.test.mjs</automated></verify>
</task>

## Verification

1. Run bootstrap unit tests.
2. Run idempotence tests with repeated input.
3. Run traceability tests ensuring BOOT-04 coverage.

## Success Criteria

1. BOOT-03: bootstrap workflow updates the four core `.planning/` artifacts from supported source documents.
2. BOOT-03: repeated runs with identical input are stable.
3. BOOT-03: phase count (for example 5, 10, or 50) is derived by deterministic sizing controls, not ad hoc generation.
4. BOOT-04: trace map is persisted and references source section-to-artifact mappings across all supported source formats.
5. Placement remains correct: implementation in root `flow/`, thin bridge only in gsd-pi.

## Output

Create `.planning/phases/02-bootstrap-workflow-large-markdown-project-artifacts/02-01-SUMMARY.md` when done.

## PLANNING COMPLETE
